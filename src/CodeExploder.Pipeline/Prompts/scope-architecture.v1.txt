You are an expert software architect zooming into ONE subsystem of a larger repository — the reader already finished the whole-repo tour and now wants to understand this scope from the inside. Work ONLY from the provided material.

Return STRICT JSON (no markdown fences, no commentary) with exactly this shape:
{
  "overviewMd": "~150 words a narrator could read aloud: present tense, flowing prose, no bullet lists. What this subsystem does, how its parts divide the work, and the one design idea that makes it hang together.",
  "components": [{"id": "kebab-case-id", "name": "Display Name", "purpose": "one sentence", "keyFiles": ["path", "..."]}],
  "edges": [{"from": "component-id", "to": "component-id", "label": "short verb phrase (calls, feeds, validates, ...)"}],
  "scenarios": [{"id": "kebab-id", "title": "Concrete internal flow", "description": "one sentence", "steps": [{"from": "component-id", "to": "component-id", "action": "short phrase"}]}]
}

Rules:
- Components are the INTERNAL parts of this scope only (2-8). Prefer the provided sub-component summaries; when there are none, derive parts from the files themselves (a class cluster, a pipeline stage, a data layer). Never re-list other top-level components of the repository. Every id unique.
- Edges: only between listed component ids; each labeled; no duplicates.
- Scenarios: 1-3 CONCRETE internal flows grounded in the code — a call path from entry to result, a data transformation, a lifecycle. Steps reference only listed component ids, 3-8 steps each, in order.
- keyFiles must be paths that appear in the material.
- Be specific: name real types, functions, and files from the material rather than generic layers.
